<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration</title>
    <style>
        .register{
            width: 400px;
            background: #fff;
            border: 1px solid #dddfe2;
            box-shadow: 0 0 15px 5px;
            border-radius: 8px;
            padding: 1rem;
            align-items: center;
            justify-content: center;
            margin: auto;
            margin-top: 20px;
        }

        .register > input{
            border-radius: 8px;
            border: 2px solid #dddfe2;
            outline: none;
            color: #1d2129;
            margin: 0.5rem 0;
            padding: 0.5rem 0.75rem;
            width: 93%;
            font-size: 1rem;
        }

        .register > .button{
            background: #1877f2;
            border: 1px solid #1877f2;
            color: #fff;
            font-size: 1.25rem;
            padding: 0.5rem;
            margin: 0.5rem 0;
            border-radius: 8px;
            outline: none;
            cursor: pointer;
            width: 100px;
        }

        .register > .button >a{
            text-decoration: none;
            color: white;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <form action="/api/register" method="post" class="register">
        <h1>Register</h1>
        <input type="email" name="email" placeholder="Your email"></input>
        <input type="password" name="password" placeholder="Your password"></input>
        <input type="text" name="qualification" placeholder="Your qualification"></input>
        <input type="text" name="city" placeholder="Your city"></input>
        <input type="tel" name="contact" placeholder="Your contact"></input>
        <button class="button" type="submit">Register</button>
        <div>or</div>
        Already have an account ? &nbsp;<a href="/api/login">Login</a>
    </form>
</body>
</html>