<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        .login{
            width: 400px;
            background: #fff;
            border: 1px solid #dddfe2;
            box-shadow: 0 0 15px 5px;
            border-radius: 8px;
            padding: 1rem;
            align-items: center;
            justify-content: center;
            margin: auto;
            margin-top: 100px;
        }

        .login > input{
            border-radius: 8px;
            border: 2px solid #dddfe2;
            outline: none;
            color: #1d2129;
            margin: 0.5rem 0;
            padding: 0.5rem 0.75rem;
            width: 93%;
            font-size: 1rem;
        }

        .login > .button{
            background: #1877f2;
            border: 1px solid #1877f2;
            color: #fff;
            font-size: 1.25rem;
            padding: 0.5rem;
            margin: 0.5rem 0;
            border-radius: 8px;
            outline: none;
            cursor: pointer;
            width: 80px;
        }

        .login > .button >a{
            text-decoration: none;
            color: white;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <form class="login">
            <h1>Login</h1>
            <input type="text" name="email" placeholder="Enter your Email"></input>
            <input type="password" name="password" placeholder="Enter your password"></input>
            <button class="button" type="submit">Login</button>
            <div>or</div>
            Dont have an account ? &nbsp;<a href="/api/register">Register</a>
    </form>
</body>
</html>